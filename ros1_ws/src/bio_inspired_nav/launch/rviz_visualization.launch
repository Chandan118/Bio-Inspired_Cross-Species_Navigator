<?xml version="1.0"?>
<launch>
  <!-- Robot State Publisher -->
  <param name="robot_description" command="$(find xacro)/xacro '$(find bio_inspired_nav)/urdf/bio_robot.urdf.xacro'" />
  
  <node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher">
    <param name="publish_frequency" value="30.0"/>
  </node>

  <!-- Joint State Publisher (for visualization) -->
  <node name="joint_state_publisher" pkg="joint_state_publisher" type="joint_state_publisher">
    <param name="use_gui" value="false"/>
  </node>

  <!-- TF from odom to base_footprint based on odometry -->
  <node name="odom_to_base_tf" pkg="bio_inspired_nav" type="odom_to_tf.py" output="screen"/>

  <!-- Launch RViz with custom config -->
  <node name="rviz" pkg="rviz" type="rviz" args="-d $(find bio_inspired_nav)/rviz/navigation.rviz" required="false"/>

</launch>
